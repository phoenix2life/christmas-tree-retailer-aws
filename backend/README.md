# Backend

AWS Lambda-compatible Python handlers for product and order APIs.

## Handlers

| Handler | Routes |
|---|---|
| `products.handler` | `GET /products`, `GET /products/{productId}` |
| `orders.handler` | `POST /orders`, `GET /orders`, `GET /orders/{orderId}` |

## Runtime

Python 3.12
